# Used by Cloudflare Workers (set these via `wrangler secret put` for production)
# For local development, create a .dev.vars file with the same keys

# R2 public URL (used to construct full image URLs)
R2_PUBLIC_URL=https://images.khurramproteins.com
